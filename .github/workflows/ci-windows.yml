# SPDX-FileCopyrightText: © 2025 Kyurenpoto <heal9179@gmail.com>
#
# SPDX-License-Identifier: MIT

---
name: Windows

on:
  push:
    branches:
      - master
      - develop
  pull_request:
    branches:
      - master
      - develop

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: install vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git
          cd vcpkg
          .\bootstrap-vcpkg.bat
          echo "VCPKG_ROOT=$(Get-Location)" >> $GITHUB_ENV
        id: vcpkg

      - name: install conan
        run: |
          choco install conan -y
          echo "CONAN_HOME=C:\ProgramData\chocolatey\lib\conan\tools\bin" >> $GITHUB_ENV
        id: conan

      - name: configure
        run: |
          cd ..
          cmake --preset x64-debug

      - name: build
        run: cmake --build --preset x64-debug -j4 --verbose

      - name: test
        run: |
          ctest --preset x64-debug -j4 --verbose --output-on-failure
